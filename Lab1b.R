# Activity 1
S <- seq(from=0, to=20, by=2)
print(S)

# Activity 2
num1 = 0.956786
num2 = 7.8345901

#round values to 2 decimal place
round(num1, digits=2)
#round values to 3 decimal place
round(num2, digits=3)

# Activity 3
radius <- readline(prompt = "Enter radius:")
radius <- as.numeric(radius)
print(pi * radius^2)